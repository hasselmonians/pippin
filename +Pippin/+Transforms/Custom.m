function [y, params] = Custom(x,funcName, params)
% Takes in a variable, a custom transformation function, and corresponding
% input paramters. At runtime, will run the custom transformation. But,
% maintains interoperability with other Pippin.Transforms



end

